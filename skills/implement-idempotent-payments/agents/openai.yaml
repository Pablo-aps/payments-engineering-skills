interface:
  display_name: "Idempotent Payments"
  short_description: "Prevent duplicate payment mutations safely"
  default_prompt: "Use $implement-idempotent-payments to make this payment mutation safe under retries and concurrency."
