interface:
  display_name: "Payment Ledger Design"
  short_description: "Design auditable, balanced payment ledgers"
  default_prompt: "Use $design-payment-ledger to run the hazard pass, then design or review this ledger with explicit invariants and failure tests."
